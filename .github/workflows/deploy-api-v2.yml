name: Deploy API (v2)

on:
  push:
    branches:
      - release/v2
    paths:
      - "server/**"

jobs:
  build-api-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Get current npm version
        id: package-version
        uses: martinbeentjes/npm-get-version-action@main
        with:
          path: server

      - name: Login to Docker Hub
        run: docker login -u ${{ secrets.ORG_DOCKER_USERNAME }} -p ${{ secrets.ORG_DOCKER_PASSWORD }}

      - name: Build Docker Image and Push To Docker Hub
        run: |
          cd server
          docker build . --file Dockerfile --tag wiseoldman/api-v2:${{steps.package-version.outputs.current-version}}
          docker push wiseoldman/api-v2:${{steps.package-version.outputs.current-version}}
  deploy:
    runs-on: ubuntu-latest
    needs: [build-api-image]
    steps:
      - name: Restart docker
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USER }}
          passphrase: ${{ secrets.DO_SSH_PASS }}
          key: ${{ secrets.DO_SSH_KEY }}
          script: |
            cd wise-old-man
            docker-compose pull api-v2:${{steps.package-version.outputs.current-version}}
            docker-compose up -d --no-deps --build api-v2
