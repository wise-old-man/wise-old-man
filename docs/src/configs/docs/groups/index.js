import entities from './entities';
import endpoints from './endpoints';

export default {
  title: 'Groups',
  url: '/docs/groups',
  description: 'A group is essentially a list of players, with roles for each player.',
  entities,
  endpoints
};
